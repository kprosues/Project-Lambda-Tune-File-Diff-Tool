// Additional electron-builder configuration
// Most config is in package.json, this file can be used for advanced settings

module.exports = {
  // Any additional electron-builder config can go here
  // The main config is in package.json "build" section
};

